<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lose.forum.dao.UserDao">

	<!-- 通用查询映射结果 -->
	<resultMap id="BaseResultMap" type="com.lose.forum.model.User">
		<id column="id" property="id" />
		<result column="name" property="name" />
		<result column="password" property="password" />
		<result column="email" property="email" />
		<result column="image" property="image" />
	</resultMap>

	<sql id="BaseColumnList">
		id as id,
		name as name,
		password as password,
		email as email,
		image as image
	</sql>

	<insert id="add" parameterType="com.lose.forum.model.User">
		insert into
		user(name,password,email,image)
		values(#{name},#{password},#{email},#{image})
	</insert>

	<select id="findUser" parameterType="com.lose.forum.model.User"
		resultMap="BaseResultMap">
		select
		<include refid="BaseColumnList" />
		from user
		where 1=1
		<if test="id!=null and id!=''">
			AND id = #{id}
		</if>
		<if test="name!=null and name!=''">
			AND name = #{name}
		</if>
	</select>

	<delete id="deleteUser" parameterType="com.lose.forum.model.User">
		delete from user
		where
		id=#{id}
	</delete>
</mapper>
